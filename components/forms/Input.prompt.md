Text field for forms — newsletter sign-up, checkout, search. Cream-friendly with a forest focus ring.

```jsx
<Input label="Correo" type="email" placeholder="tu@correo.cl" hint="Te avisamos de nuevos cafés." />
```

Supports `label`, `hint`, `error` (red state), and `iconLeft`. Accepts all native input attributes.
